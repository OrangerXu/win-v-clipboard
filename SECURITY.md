# 隐私与安全

## 剪贴板不是安全存储

历史文件以明文 JSON 保存，可能包含密码、令牌、个人信息或私有图片。当前没有加密、敏感内容识别或按应用排除机制。复制敏感信息前请暂停监听；删除某条历史不保证它从操作系统、备份或磁盘恢复工具中不可恢复。

不要把真实 `clipboard-history.json`、用户数据目录、登录凭据、日志或包含私密内容的截图上传到仓库或 Issue。`.gitignore` 是防误操作措施，不是数据脱敏工具。

## 自动粘贴边界

此应用需要访问系统剪贴板并向 X11 目标窗口注入按键。它会在自动粘贴前检查窗口和输入焦点，但不能替代用户确认目标应用是否可信。粘贴命令或多行文本前，应理解终端自身对粘贴内容的处理行为。

保留 Electron 渲染器沙箱、上下文隔离及 CSP。Ubuntu 的安装许可只应匹配实际应用的完整路径，不要关闭全系统 AppArmor 或长期使用 `--no-sandbox`。

## 报告问题

请向仓库所有者私下报告潜在安全问题。使用合成内容和最小复现步骤，不附真实剪贴板历史、访问令牌或账号密码。
